{
  "$schema": "https://schema.management.azure.com/schemas/2015-01-01/deploymentTemplate.json#",
  "contentVersion": "1.0.0.0",
  "parameters": {
    "parameters": {
      "projectName": {
        "type": "string",
        "minLength": 2,
        "metadata": {
          "description": "Short description of the project"
        }
      }
    }
  },
  "variables": {
  },
  "functions": [
  ],
  "resources": [
    {
      "apiVersion": "2017-05-10",
      "name": "bm-namingconvention-gov",
      "type": "Microsoft.Resources/deployments",
      "properties": {
        "mode": "Incremental",
        "templateLink": {
          "uri": "[uri(deployment().properties.templateLink.uri, '/namingConvention/namingConvention.json')]",
          "contentVersion": "1.0.0.0"
        },
        "parameters": {
          "projectName": {
            "value": "[parameters('projectName')]"
          }
        }
      }
    }

  ],
  "outputs": {
    "defaultConvention": {
      "type": "string",
      "value": "[reference('bm-namingconvention-gov').outputs.defaultConvention.value]"
    },
    "storageConvention": {
      "type": "string",
      "value": "[reference('bm-namingconvention-gov').outputs.storageConvention.value]"
    }
  }
}